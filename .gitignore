__pycache__/
*.py[cod]
*.so
*.dylib

# 编译产物
tweak/build/
build/
dist/

# zsign 缓存（自动重建的派生文件，非密钥）
.zsign_cache/

# 虚拟环境
.venv/
venv/

# 本地私密文件：证书、描述文件、IPA
*.ipa
*.p12
*.mobileprovision
*.cer
*.key

# 云构建配置（含 GitHub / Codemagic Token，切勿提交；代码里也已禁止上传）
importer_config.json

.DS_Store

# 本地验证用临时产物
sim_*/
simrun*.txt
.tmp-*

# 从手机上导出来的日志
*.log

# 本地调试脚本（非发布内容）
decode_prov.py
diag_sign.py
